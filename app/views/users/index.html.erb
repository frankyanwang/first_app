<% title "Users" %>

<table>
  <tr>
    <th>Email</th>
    <th>Username</th>
  </tr>
  <% for user in @users %>
	<tr>
		<td><%= user.email %></td>
		<td><%= user.username %></td>
		<% if user.is_being_followed %>
			<td><%= link_to "unfollow", user.is_being_followed, :method => :delete %></td>
		<% else %>
			<td><%= link_to "follow", followships_path(:follower_id => user.id), :method => :post %></td>	
		<% end %>
		<td><%= link_to "Show", user %></td>
	</tr>		
  <% end %>
</table>